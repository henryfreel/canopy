<!-- navbar -->
  <nav class="navbar navbar-default navbar-fixed-top" id="navbar">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a href="/" class="navbar-brand" id="navbar-brand"><div id="navbar-logo"></div>
          <div id="navbar-name">CANOPY</div>
        </a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><a id="<%= @projects_page ? 'active-page' : ''%>" href="/projects">Projects</a></li>
          <li><a id="<%= @users_page ? 'active-page' : ''%>" href="/users">Developers</a></li>
          <li><a id="<%= @about_page ? 'active-page' : ''%>" href="/about">About</a></li>
        </ul>

        <ul id="navbar-right" class="nav navbar-nav">

          <% if current_user %>
          <!-- if user logs in -->

            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <div id="navbar-avatar" style="background-image: url(<%= current_user.avatar %>);"></div>
                <div id="navbar-username"><%= current_user.username %></div>
              </a>
              <ul class="dropdown-menu pull-right">
                <li>
                  <a href="/canopy-<%= current_user.username %>"><span class="fa fa-user fa-fw profile-info-icon"></span> Profile</a>
                </li>
                <li>
                  <a href="/"><span class="fa fa-cog fa-fw profile-info-icon"></span> Settings</a>
                </li>
                <li role="separator" class="divider"></li>
                <li>
                  <a href="/logout"><span class="fa fa-sign-out fa-fw profile-info-icon"></span> Logout</a>
                </li>
              </ul>
            </li>
            
            <li>
              <a class="btn btn-primary navbar-action" href="/projects/new"><span class="fa fa-leaf" aria-hidden="true"></span> &nbspNew Project</a>
            </li>

          <% else %>
            <!-- if no user -->

            <li><a href="/login" class="btn btn-default">Log in</a></li>
            <li><a href="/signup" class="btn btn-primary">Sign up</a></li>

          <% end %> 
        </ul>

      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>