== "Project Snorkel"

When you first clone locally, first create and migrate the database (in terminal: 'rake db: create' and then rake db:migrate) and get all the gems you'll need (in terminal: 'bundle install').
